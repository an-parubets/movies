import { StyleSheet } from 'aphrodite';

export const styles = StyleSheet.create({
    movies: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'center',
        width: '100%'
    }
});
